/* global window */

// var lodash;

// if (require) {
//   try {
//     lodash = require("lodash");
//   } catch (e) {}
// }

// if (!lodash) {
//   lodash = window._;
// }

// module.exports = lodash;

const lodash = require('lodash')

module.exports = lodash
